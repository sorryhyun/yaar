{
  "name": "@yaar/tests",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "bun run ../../scripts/build/ensure-deps-built.ts shared compiler",
    "test": "bun test",
    "bench": "bun run src/benchmarks/context-tape.bench.ts && bun run src/benchmarks/fingerprint.bench.ts && bun run src/benchmarks/startup.bench.ts",
    "pretypecheck": "bun run ../../scripts/build/ensure-deps-built.ts shared compiler",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@yaar/server": "workspace:*",
    "@yaar/shared": "workspace:*",
    "bun-types": "catalog:",
    "jsdom": "^29.1.1",
    "mitata": "^1.0.34"
  }
}
