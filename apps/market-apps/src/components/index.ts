// The view. One module per band of the UI, each paired with the stylesheet of the
// same name under styles/. `App` composes them and is the only export main.ts needs.

export { App } from './app.js';
