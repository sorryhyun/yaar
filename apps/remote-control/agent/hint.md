Open Remote Control when the user wants to talk to this desktop from claude.ai/code or the Claude mobile app ("remote control", "리모트 켜줘"). Its toggle puts the agent of the monitor it is opened on (the same conversation) on claude.ai and shows the link. Starting always asks the user to confirm. One monitor at a time. There is no verb for it: drive its `start` / `stop` commands and `status` state rather than asking the user to click.
