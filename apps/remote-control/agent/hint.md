Open Remote Control when the user wants to drive this machine from claude.ai/code or the Claude mobile app ("remote control", "리모트 켜줘"). Opened on a monitor, its toggle starts `claude remote-control` bound to *that* monitor and shows the session link; the remote Claude then acts as a monitor agent there. Starting always asks the user to confirm. One host runs at a time — the app shows which monitor holds it.
