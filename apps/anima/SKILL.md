# Anima (WebGPU)

A compiled TypeScript application.

## Launch
Open this app in an iframe window:
```
invoke('yaar://windows/anima', {
  action: "create",
  title: "Anima (WebGPU)",
  renderer: "iframe",
  content: "yaar://apps/anima"
})
```
