{
  "name": "yaar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reactive AI interface where the AI decides what to show and do next",
  "scripts": {
    "dev": "./scripts/dev.sh",
    "dev:server": "pnpm --filter @yaar/server dev",
    "dev:frontend": "pnpm --filter @yaar/frontend dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:frontend": "pnpm --filter @yaar/frontend test",
    "test:server": "pnpm --filter @yaar/server test",
    "test:shared": "pnpm --filter @yaar/shared test",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules dist",
    "build:exe": "pnpm build:exe:prepare && pnpm build:exe:bundle",
    "build:exe:prepare": "pnpm -r build",
    "build:exe:bundle": "node scripts/build-exe-bundle.js --provider claude --target windows && node scripts/build-exe-bundle.js --provider codex --target windows",
    "build:exe:bundle:linux": "node scripts/build-exe-bundle.js --provider claude --target linux && node scripts/build-exe-bundle.js --provider codex --target linux",
    "build:exe:bundle:macos": "node scripts/build-exe-bundle.js --provider claude --target macos && node scripts/build-exe-bundle.js --provider codex --target macos",
    "build:exe:assets": "node scripts/copy-exe-assets.js"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "devDependencies": {
    "node-poppler-win32": "1.0.2",
    "xlsx": "^0.18.5"
  }
}
