1. optimize system and agent profile prompt. subagent prompts are not optimized
2. harden session log / caching features. currently those are broken.
3. app market publisher / app submission / download in secure. authentication for users
4. decision on window agent's boundary. should it scoped to its window only?
5. harden agent-to-agent, multi-monitor features.
99. sqlite-based in-memory optimization, mobile remote friendly, admin agent (cross-monitor manageable), 